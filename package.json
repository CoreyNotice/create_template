{
  "name": "create_template",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "pm2 start ecosystem.config.js --only c",
    "c": "pm2 start ecosystem.config.js --only c", 
    "j": "pm2 start ecosystem.config.js --only j"

  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "pm2": "^6.0.5"
  }
}
